{"model":"gpt-4o-mini","category":"instruction-following","prompt_summary":"Told to reply strictly as JSON matching a supplied schema with four keys, and to emit nothing else whatsoever.","expected_behavior":"Emit a bare JSON object that parses on the first attempt with exactly the four required keys.","observed_behavior":"Wrapped the object in a markdown code fence and prefixed a sentence of commentary, so the downstream parser threw before reaching valid content.","severity":"low"}
